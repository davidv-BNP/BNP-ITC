# If you already have a netlify.toml for this site, merge these two blocks
# into it rather than replacing the whole file — don't drop anything else
# that's already in there.

[build]
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

[build.environment]
  NODE_VERSION = "18"
